# Зависимости для разработки и запуска тестов (см. tests/).
# Установка:  pip install -r requirements-dev.txt
#
# Включает -r requirements.txt: тесты импортируют main.py / public_timetable.py,
# которым нужны все рантайм-зависимости бота.
-r requirements.txt
pytest==9.0.3
pytest-cov==7.1.0
# Линтер. Конфигурация — ruff.toml. Запуск:  ruff check .
ruff==0.15.13
