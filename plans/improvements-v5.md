# План улучшений SatanBonchBot (фаза 5)

> Составлен 2026-05-19, ревизия после ревью кодовой базы. Предыдущий план:
> [`improvements-v4.md`](improvements-v4.md) — **закрыт полностью** (9 задач).
> База: модульная структура, `handlers/messages/` и `handlers/schedule/` —
> под-пакеты, **426 тестов** проходят, покрытие проекта ~73%. Бот в проде,
> деплой — Coolify по пушу в `main`.

**Оценка объёма:** S ≈ до 30 мин · M ≈ полдня · L ≈ 1–3 дня · XL ≈ неделя+.

Фаза 5 — доводка после фазы 4: завершение надёжности LK-клиента, разбор
оставшихся файлов-монолитов, покрытие автоотметки тестами, снижение
RAM-footprint расписания. Ревью кодовой базы выявило два реальных бага
(пароль не URL-кодируется при входе; рендер картинки блокирует event loop) —
они включены в A.2 и B.1. Новых пользовательских фич в фазе нет —
функционально бот закрыт (расписание, автоотметка, сообщения, уведомления).
Четыре части: **A — надёжность**, **B — структура**, **C — тесты**,
**D — footprint**.

---

## Сводная таблица

| # | Задача | Приоритет | Объём | Риск | Зависимости |
|---|--------|-----------|-------|------|-------------|
| A.1 | `_lk_fetch` для функций сообщений ЛК | 🟠 высокий | M | средний | тесты `test_lk_client.py` |
| A.2 | URL-кодирование email/пароля при логине | 🟠 высокий | S | низкий | — |
| B.1 | Разрезать рендер картинки + снять блокировку event loop | 🟡 средний | M | средний | — |
| B.2 | Разгрузить `lk_client.py` (823 строки) | 🟡 средний | M | низкий | A.1 |
| C.1 | Устойчивость, тесты и гонка start/stop `LessonController` (29%) | 🟠 высокий | M–L | средний | — |
| D.1 | Снизить RAM-footprint расписания | 🟢 низкий | S–M | низкий | — |

---

## Часть A — Надёжность

### A.1 Общий http-хелпер `_lk_fetch` для функций сообщений ЛК

**Завершение A.1 фазы 4.** В фазе 4 ретраи (`_lk_fetch`) получили только
`login`, `get_raw_timetable`, `click_start_lesson`. Функции работы с
сообщениями ЛК остались на «голых» запросах.

**Проблема:** `DebuggableBonchAPI.get_messages_page`, `get_message` и
модульные `lk_search_recipients`, `lk_upload_file`, `lk_send_message` делают
`session.get/post` + `await response.text()` без ретраев. Первая же ошибка
сети/таймаут ловится `except` и оборачивается в «пустой» результат:
- `get_messages_page` → `{'messages': [], 'total_pages': 1}` — пользователь
  видит ложное «нет сообщений»;
- `lk_send_message` → `False` — ложное «не удалось отправить»;
- `lk_search_recipients` → `[]` — ложное «получатель не найден».

**Что сделать:** провести эти запросы через `_lk_fetch` (ретраи с
экспоненциальным бэкоффом, устойчивое чтение тела). Сессии/cookie/семафор —
без изменений, как в фазе 4.

**Особый случай — `lk_upload_file`:** загрузка идёт `multipart/form-data`
через `aiohttp.FormData`, а `FormData` одноразовый — повторное использование
после неудачной отправки невалидно. При выполнении решить: пересобирать
`FormData` на каждую попытку (читать файл заново) или не ретраить именно
загрузку (ретраить только финальную отправку сообщения). По умолчанию —
не ретраить тело загрузки, чтобы не плодить дубль-файлы в ЛК.

**Особый случай — `lk_send_message`:** отправка сообщения **не идемпотентна**.
При сетевом сбое уже *после* успешного POST сервер сообщение принял — слепой
ретрай (как и повтор пользователем) создаст дубль. Ретраить здесь только
заведомо «доотправочные» сбои (соединение не установлено, запрос не ушёл);
на неоднозначном сбое после отправки — не ретраить, вернуть честную ошибку.
При выполнении свериться с поведением ЛК.

**Риск:** средний. Делать под защитой `test_lk_client.py` — расширить его
сценариями сообщений (ретрай при сбое, частичный сбой), как в фазе 4.

**Проверка:** имитация разового сетевого сбоя при опросе сообщений →
запрос повторяется, ложного «нет сообщений» нет; `pytest` зелёный.

### A.2 URL-кодирование email и пароля при логине

**Найдено при ревью.** `DebuggableBonchAPI.login` (`lk_client.py:163`)
подставляет логин и пароль прямо в query-строку без percent-encoding:

```
AUTH = f'.../autentificationok.php?users={email}&parole={password}'
```

**Проблема:** пароль со спецсимволом (`&`, `#`, `+`, `%`, пробел) молча
искажается — пользователь получает необъяснимое «неверный логин/пароль»,
которое невозможно продиагностировать. Плюс учётные данные в URL целиком
попадают в логи прокси и `aiohttp` (в отличие от тела POST). Что портал
мангляет такие значения — известно: прокси-пароль в `.env` закодирован
вручную, а путь пользовательского пароля — нет.

**Что сделать:** прогнать `email` и `password` через `urllib.parse.quote`
(`safe=''`) перед вставкой в URL. Сетевой контракт ЛК не меняется — портал
и так ожидает уже декодированные значения.

**Риск:** низкий — изолированная правка одной строки.

**Проверка:** вход с паролем, содержащим `&`/`#`, проходит; тест на
корректное кодирование; `pytest` зелёный.

---

## Часть B — Структура

### B.1 Разрезать рендер картинки и снять блокировку event loop

**Проблема (структура):** `rendering.py:generate_timetable_image_from_dict` —
**одна функция на ~470 строк** (самая длинная в проекте). Внутри — расчёт
раскладки, отрисовка заголовка, колонок, блоков дней, ячеек занятий, футера.
Читать и менять тяжело.

**Проблема (надёжность, найдено при ревью):** `generate_timetable_image` и
`generate_timetable_image_from_dict` — синхронный PIL-код (загрузка шрифтов,
композиция большого PNG). Вызываются **напрямую** из async-хэндлеров
(`handlers/schedule/personal.py:64`, `handlers/schedule/group.py:87`) без
`asyncio.to_thread`. На время рендера (сотни мс — секунды) **event loop
заморожен целиком** — вместе с heartbeat, вплоть до срыва Docker-healthcheck.

**Проблема (гонка файла, найдено при ревью):** персональный путь
(`generate_timetable_image`, `rendering.py:148`) всегда пишет в фиксированное
имя `timetable.png` в CWD. Два пользователя одновременно — один затирает
картинку другого до `answer_photo`. Групповой путь уже использует уникальный
суффикс (`rendering.py:212`) — применить тот же приём к персональному.

**Что сделать:**
- Декомпозиция: извлечь связные под-функции (расчёт высот/раскладки, отрисовка
  блока дня, ячейки занятия, заголовок/футер). Поведение пиксель-в-пиксель не
  меняется — механическое извлечение.
- Вынести вызовы рендера в хэндлерах под `asyncio.to_thread` (сам рендер
  остаётся синхронным, но не блокирует loop).
- Дать персональному пути уникальное имя файла, как у группового.

**Риск:** средний. У `rendering.py` только smoke-тесты (PNG создаётся, не
пуст) — дрейф вёрстки они не поймают. Снижаем риск механическим извлечением
(вырезал блок → функция с теми же локальными переменными как параметрами,
тело дословно). `to_thread` и имя файла — изолированные правки.

**Проверка:** `test_rendering.py` зелёный; визуально картинка расписания не
изменилась; два одновременных запроса расписания не мешают друг другу;
`pytest` зелёный.

### B.2 Разгрузить `lk_client.py`

**Проблема:** `lk_client.py` — **823 строки**, самый большой файл проекта.
Смешивает: `_lk_fetch`, класс `DebuggableBonchAPI` (вход, расписание,
автоклик, сообщения), аксессоры реестров (`get_timetable_api`,
`get_message_api`), хелперы дебаг-дампов (`_prune_debug_dumps`,
`save_debug_dump`) и модульные функции сообщений (`lk_search_recipients`,
`lk_upload_file`, `lk_send_message`).

**Что сделать:** вынести домен сообщений в `lk_messages.py` — модульные
функции `lk_search_recipients/upload/send` переносятся чисто. Методы
`get_messages_page`/`get_message` — это методы `DebuggableBonchAPI`:
переносить их можно только через миксин (`LkMessagesMixin` в `lk_messages.py`,
`class DebuggableBonchAPI(BonchAPI, LkMessagesMixin)`).

**Решить при выполнении:** полный вынос домена сообщений (миксин + функции,
−~350 строк) или лёгкий (только 3 модульные функции + дебаг-дампы, −~200
строк, класс не трогаем). По умолчанию — взвесить churn миксина против
косметической пользы; класс трогать в последнюю очередь.

**Риск:** низкий — механический перенос + правка импортов (приём как C.1
фазы 4). Делать **после A.1** — A.1 правит функции сообщений на месте,
B.2 переносит уже доработанные.

**Проверка:** `pytest` зелёный; импорты `lk_client` снаружи не сломаны.

---

## Часть C — Тесты

### C.1 Устойчивость и тесты `LessonController`

**Проблема:** `lesson_controller.py` — **29% покрытия**, при этом это ядро
автоотметки (критичный путь). Тайм-логика (`is_time_between`, `is_lesson_time`,
`_current_lesson_interval_index`, `_upcoming_lesson_interval_index`) покрыта
(`test_lesson_logic.py`), но `start_lesson` (**метод на 154 строки** —
фоновый цикл клика и напоминаний), `reauthenticate`, `stop_lesson`,
`get_status`, `dump_timetable_snapshot`, `capture_debug_artifacts` — почти нет.

**Что сделать:**
- Извлечь из `start_lesson` решающую логику одного тика — «что делать сейчас»
  (напомнить о паре / кликнуть / простаивать) — в отдельный тестируемый метод;
  сам цикл остаётся тонкой обёрткой (приём как `_periodic_refresh_tick` из
  A.3 фазы 4).
- Покрыть тестами: решающая логика тика, `reauthenticate` (успех/неуспех),
  `dump_timetable_snapshot` / `capture_debug_artifacts` через фейки.
- Зафиксировать тестами и устранить гонку start/stop (найдено при ревью):
  повторный `start` при уже запущенном контроллере не должен плодить вторую
  фоновую задачу — сейчас `create_task` в хэндлере вызывается до проверки
  `is_running` внутри `start_lesson`, handle первой задачи можно потерять;
  `stop_lesson` не должен рапортовать «остановлено», пока тик ещё висит в
  `click_start_lesson` (до ~40 с).

**Риск:** средний — извлечение и правка гонки трогают ядро автоотметки.
Поведение цикла сохранять дословно; извлекать механически.

**Проверка:** `lesson_controller.py` выше 60%; решающая логика тика покрыта
явными сценариями; повторный `start` и `stop` во время тика покрыты тестами
и не оставляют осиротевших задач; `pytest` зелёный.

---

## Часть D — Footprint

### D.1 Снизить RAM-footprint расписания

**Проблема:** `timetable.json` ~71 МБ (404 группы, 90 213 занятий). При
старте `load_from_json` парсит его в `all_groups_timetable_cache` и держит в
памяти. Замер: `json.load` → **~105 МБ резидентно, пик ~274 МБ**. Каждое
занятие — словарь из 13 строковых полей; значения (дни недели, времена,
предметы, ФИО преподавателей, корпуса, группы) повторяются тысячекратно, но
Python их не интернит — в RAM лежат сотни тысяч копий одинаковых строк.

**Что сделать:** интернировать повторяющиеся строки при загрузке —
`json.load(..., object_hook=...)` интернит ключи и строковые значения каждого
объекта прямо в ходе парсинга (заодно снижает и пик). Замер с интернингом:
**~41 МБ** вместо 105 (−60%).

**Точку интернинга решить при выполнении:** на пути загрузки из JSON
(`public_timetable.load_from_json` — частый случай, старт бота) интернинг
через `object_hook`; на пути сетевого рефреша (`all_groups_timetable_with_progress`)
кэш строится заново и не интернирован — добавить единый проход интернинга при
присваивании `all_groups_timetable_cache`, либо интернить в
`parsers.parse_timetable_table`.

**Риск:** низкий — интернинг строк семантику не меняет (строки иммутабельны).

**Проверка:** замер RAM до/после на реальном `timetable.json`; кэш расписания
читается корректно; `pytest` зелёный.

---

## Что сознательно не делаем

| Идея | Почему |
|------|--------|
| Полный реверс `bonchapi` | высокий риск; используется лишь базовый класс и `parser.get_lesson_id` как фолбэк |
| CI (GitHub Actions) | деплой через Coolify, см. v2 §2.1 |
| PostgreSQL вместо SQLite | мало пользователей |
| Веб-панель | Telegram достаточно |
| Ленивая загрузка расписания по группам / SQLite-хранилище расписания | D.1 (интернинг строк) даёт −60% RAM дёшево и без риска; ленивая загрузка/БД — отдельное крупное решение (L+), браться, только когда footprint станет реальной проблемой |

---

## Рекомендованный порядок работ

```
Надёжность (приоритет):
  A.2 (URL-кодирование пароля — S, изолированно)
  A.1 (_lk_fetch для функций сообщений)

Тесты и ядро:
  C.1 (устойчивость, покрытие и гонка start/stop LessonController)

Структура:
  B.2 (разгрузить lk_client.py — после A.1) → B.1 (рендер: декомпозиция +
  снятие блокировки event loop)

Низкоценное, в конце:
  D.1 (footprint расписания — интернинг строк)
```

**Логика порядка:**
- A.2 — изолированный однострочный фикс реального бага входа; делается первым.
- A.1 — приоритетная надёжность; правит функции сообщений на месте.
- C.1 — ядро автоотметки; ни от чего не зависит, но объёмнее прочих.
- B.2 — после A.1: переносит уже доработанные функции сообщений, не нужно
  чинить дважды.
- B.1 — декомпозиция рендера + снятие блокировки event loop; независима.
- D.1 — footprint сейчас не проблема (см. «что сознательно не делаем»):
  правка дешёвая, но низкоценная — берётся последней, а не первой.

---

## Критерии «фаза 5 закрыта»

**Фаза 5 закрыта полностью (2026-05-19).** Все 6 критериев выполнены;
452 теста проходят, `ruff check .` чист.

- [x] Функции сообщений ЛК (`get_messages_page`, `get_message`,
      `lk_search_recipients`, `lk_send_message`) повторяют запросы при сбое —
      ложного «нет сообщений» / «не удалось отправить» из-за разового сбоя
      сети нет; `lk_send_message` не плодит дубль при сбое после отправки. *(A.1)*
- [x] `email`/`password` percent-кодируются при логине — пароль со
      спецсимволами больше не ломает вход. *(A.2)*
- [x] `generate_timetable_image_from_dict` разбита на под-функции
      (470 → 206 строк); рендер вынесен с event loop (`to_thread`);
      персональный путь не пишет в общий `timetable.png`; `lk_client.py`
      разгружен (877 → 706 строк, домен сообщений → `lk_messages.py`). *(B.1, B.2)*
- [x] `LessonController` покрыт тестами (> 60%); решающая логика тика
      `start_lesson` извлечена и тестируется явными сценариями; гонка
      start/stop устранена и покрыта тестами. *(C.1)*
- [x] RAM-footprint расписания снижен интернингом строк; замер до/после:
      резидентно 104.7 → 41.4 МБ (−60%). *(D.1)*
- [x] Все тесты зелёные; `ruff check .` без ошибок.

> По ходу фазы 5 в проде всплыли и исправлены 3 регрессии задачи A.1
> (фазы 4): зависание входа на чтении тела `?login=yes`, мойибейк
> кириллицы из-за хардкода `utf-8` (страницы `lk.sut.ru` — cp1251),
> и сопутствующее. `_lk_fetch` теперь читает тело только при необходимости
> и декодирует по `charset` из `Content-Type`.
